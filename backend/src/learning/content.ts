import type { CourseDefinition } from './types';

/** Authored server-only teaching material. Public DTOs must omit answer keys and explanations. */
export const learningCourses: CourseDefinition[] = [
  {
    id: 'python-reliable-functions',
    event_id: 'EV_012',
    title: 'Демо-модуль: надёжные функции Python',
    summary: 'За 7 минут разберите общее состояние, границу между типами и проверками и тесты крайних случаев. Авторский демо-фрагмент для Advanced Python: он не заменяет полный курс на 24 часа.',
    estimated_minutes: 7,
    version: 1,
    passing_score: 100,
    lessons: [
      {
        id: 'python-state',
        title: '1. Один вызов — своё состояние',
        blocks: [
          { type: 'paragraph', text: 'Представьте функцию, которая собирает навыки для карточки сотрудника. После первого вызова всё выглядит правильно. При обработке второго сотрудника внезапно появляется навык первого. Причина может быть в списке, записанном прямо в аргументе по умолчанию. Примеры ниже используют синтаксис Python 3.10+.' },
          { type: 'code', language: 'python', code: 'def remember(skill, items=[]):\n    items.append(skill)\n    return len(items)\n\nprint(remember("Python"))  # 1\nprint(remember("SQL"))     # 2: тот же список' },
          { type: 'paragraph', text: 'Python вычисляет значение аргумента по умолчанию при определении функции, а не при каждом вызове. Изменяемый список сохраняется между вызовами без явно переданного items. Для независимого состояния используйте None и создавайте список внутри функции.' },
          { type: 'code', language: 'python', code: 'def add_skill(\n    skill: str, skills: list[str] | None = None\n) -> list[str]:\n    result = [] if skills is None else skills.copy()\n    result.append(skill)\n    return result\n\noriginal = ["SQL"]\nupdated = add_skill("Python", original)\n# original == ["SQL"]\n# updated == ["SQL", "Python"]' },
          { type: 'callout', text: 'Здесь два отдельных решения: None убирает общий список по умолчанию; copy() сохраняет переданный список без изменений. Копия поверхностная — для вложенных изменяемых объектов правила владения нужно продумать отдельно.' },
        ],
      },
      {
        id: 'python-inputs',
        title: '2. Аннотация — договор, проверка — действие',
        blocks: [
          { type: 'paragraph', text: 'Запись value: int помогает редактору и статическому анализатору находить ошибки. Сам интерпретатор не превращает аргумент в число и не отклоняет его только из-за аннотации. На границе приложения данные из JSON нужно проверить явно.' },
          { type: 'code', language: 'python', code: 'def echo(value: int) -> int:\n    return value\n\necho("3")  # Во время исполнения вернёт строку "3".\n# Проверяющий типы инструмент может предупредить заранее.' },
          { type: 'paragraph', text: 'Договоримся, что уровень навыка — целое число от 0 до 5 включительно. Строки, дроби и логические значения не принимаются. Проверка типа идёт до сравнения границ; вызывающий код получает понятную причину отказа.' },
          { type: 'code', language: 'python', code: 'def checked_level(value: object) -> int:\n    if type(value) is not int:\n        raise TypeError("level must be an integer")\n    if not 0 <= value <= 5:\n        raise ValueError("level must be between 0 and 5")\n    return value' },
          { type: 'callout', text: 'Проверка type(value) is int здесь намеренная: bool — подкласс int, но True не является уровнем навыка по нашему договору. Для другого продукта правила преобразования могут отличаться; сначала определите их, затем пишите код.' },
        ],
      },
      {
        id: 'python-tests',
        title: '3. Проверяйте границы и отказ',
        blocks: [
          { type: 'paragraph', text: 'Проверка только checked_level(3) не заметит ошибку «меньше 5» вместо «меньше или равно 5». Хороший небольшой набор охватывает разрешённые границы, значения сразу за ними и неправильные типы. Ожидаемое исключение тоже часть поведения функции.' },
          { type: 'code', language: 'python', code: 'import unittest\n\nclass LevelTests(unittest.TestCase):\n    def test_allowed_edges(self):\n        self.assertEqual(checked_level(0), 0)\n        self.assertEqual(checked_level(5), 5)\n\n    def test_outside_range(self):\n        for value in (-1, 6):\n            with self.subTest(value=value):\n                with self.assertRaises(ValueError):\n                    checked_level(value)\n\n    def test_wrong_types(self):\n        for value in ("3", True):\n            with self.subTest(value=value):\n                with self.assertRaises(TypeError):\n                    checked_level(value)' },
          { type: 'bullets', items: ['Для списка вызовите функцию дважды: результат второго вызова не должен зависеть от первого.', 'Если функция обещает сохранить аргумент без изменений, проверьте исходный список после вызова.', 'Проверяйте результат или конкретный тип исключения, а не только отсутствие падения.'] },
          { type: 'callout', text: 'В этом модуле код служит примером для чтения; устанавливать Python не требуется. Три вопроса проверят понимание этих решений. Пройденный демо-модуль не подтверждает освоение всего 24-часового курса.' },
        ],
      },
    ],
    questions: [
      {
        id: 'python-default-state',
        prompt: 'Функция remember(value, items=[]) добавляет value в items и возвращает len(items). Сначала вызвали remember("A"), затем remember("B"), не передавая items. Какие два числа она вернёт?',
        options: [
          { id: 'fresh-each-time', text: '1 и 1: Python создаёт список при каждом вызове.' },
          { id: 'shared-default', text: '1 и 2: оба вызова используют один список по умолчанию.' },
          { id: 'both-two', text: '2 и 2: первый числовой результат изменится вместе со списком.' },
          { id: 'default-error', text: 'Возникнет ошибка: список нельзя использовать как аргумент по умолчанию.' },
        ],
        correct_option_id: 'shared-default',
        explanation: 'Список создаётся один раз при определении функции и накапливает оба элемента. Первый возвращённый результат — отдельное число 1; второй — 2. Значение None и создание списка внутри вызова устраняют общий изменяемый объект.',
      },
      {
        id: 'python-runtime-types',
        prompt: 'В функции def echo(value: int) -> int единственная строка тела — return value. Что произойдёт при echo("3") в обычном Python без дополнительной проверки типов во время исполнения?',
        options: [
          { id: 'coerced-int', text: 'Строка автоматически преобразуется в число 3.' },
          { id: 'annotation-error', text: 'Аннотация автоматически вызовет TypeError до входа в функцию.' },
          { id: 'string-return', text: 'Функция вернёт строку "3"; аннотация сама не проверяет и не преобразует аргумент.' },
          { id: 'none-return', text: 'Функция вернёт None, потому что значение не соответствует аннотации.' },
        ],
        correct_option_id: 'string-return',
        explanation: 'Аннотации описывают ожидаемые типы для инструментов, но сами не обеспечивают проверку аргументов при исполнении. Здесь нет ни преобразования, ни проверки: возвращается переданная строка. Для внешних данных нужна явная валидация.',
      },
      {
        id: 'python-boundary-tests',
        prompt: 'checked_level должен принимать только значения с type(value) is int в диапазоне 0–5 включительно. Какой набор проверяет обе разрешённые границы, оба выхода за диапазон и два неправильных типа?',
        options: [
          { id: 'middle-only', text: '1, 2 и 3 возвращают такие же числа.' },
          { id: 'wrong-boundary', text: '0 возвращает 0; 5 и 6 вызывают ValueError; "3" принимается.' },
          { id: 'annotation-only', text: 'Достаточно проверить, что у функции есть аннотация -> int.' },
          { id: 'all-edges', text: '0 и 5 принимаются; -1 и 6 вызывают ValueError; "3" и True вызывают TypeError.' },
        ],
        correct_option_id: 'all-edges',
        explanation: 'Этот набор проверяет включительность обеих границ, отказ за каждой границей и правило типов. True отклоняется выбранной проверкой точного типа. Проверки середины диапазона не обнаружат ошибку в обработке 0 или 5.',
      },
    ],
    sources: [
      { title: 'Python: значения аргументов по умолчанию', url: 'https://docs.python.org/3/tutorial/controlflow.html#default-argument-values' },
      { title: 'Python: аннотации и проверка типов', url: 'https://docs.python.org/3/library/typing.html' },
      { title: 'Python: встроенные типы, включая bool', url: 'https://docs.python.org/3/library/stdtypes.html#boolean-type-bool' },
      { title: 'Python unittest: проверка результатов и исключений', url: 'https://docs.python.org/3/library/unittest.html#unittest.TestCase.assertRaises' },
    ],
  },
  {
    id: 'system-design-reliable-requests',
    event_id: 'EV_005',
    title: 'Демо-модуль: запрос, данные и надёжный повтор',
    summary: 'За 7 минут спроектируйте путь изменения прогресса: API, транзакция, свежесть кэша и безопасный повтор запроса. Авторский демо-фрагмент System Design Fundamentals; полный курс на 16 часов он не заменяет.',
    estimated_minutes: 7,
    version: 1,
    passing_score: 100,
    lessons: [
      {
        id: 'design-request-path',
        title: '1. От кнопки до сохранённого результата',
        blocks: [
          { type: 'paragraph', text: 'Возьмём учебный сервис развития. Пользователь нажал «Завершить шаг». Браузер отправляет намерение, например ID активности. Сервер проверяет пользователя и допустимость действия, рассчитывает изменение и сохраняет результат. Передавать с клиента готовое число «добавить 100 баллов» как доверенное значение нельзя.' },
          { type: 'code', language: 'text', code: 'Браузер\n  ↓ намерение: завершить активность\nAPI: входные данные + права\n  ↓\nБизнес-правила: допуск + эффект\n  ↓\nБаза данных: изменение + запись о выполнении\n  ↓ COMMIT\nОтвет API → обновлённый экран' },
          { type: 'paragraph', text: 'Сделаем базу источником сохранённого прогресса. Изменение навыка и запись о выполнении должны попасть в одну транзакцию: либо сохраняются оба, либо не сохраняется ни один. Успешный ответ отправляем после фиксации. Иначе интерфейс может показать результат, которого в базе ещё нет.' },
          { type: 'callout', text: 'Транзакция объединяет изменения внутри базы. Она сама по себе не обновляет браузер, внешний кэш или почтовый сервис. Эти границы нужно проектировать отдельно.' },
        ],
      },
      {
        id: 'design-cache-freshness',
        title: '2. Быстрый ответ может оказаться старым',
        blocks: [
          { type: 'paragraph', text: 'Если чтений много, можно хранить копию профиля в кэше. На попадании отвечаем быстрее; на промахе читаем базу и заполняем кэш. Цена такого ускорения — отдельная задача свежести: после изменения в базе старая копия не исчезает автоматически.' },
          { type: 'code', language: 'text', code: 't=0 с   в кэш положили прогресс 40%, TTL=60 с\nt=20 с  база уже хранит 50%; кэш не удалили\nt=21 с  чтение из кэша всё ещё может показать 40%\nt=60 с  срок копии истёк; следующее чтение идёт в базу\n\nУсловие примера: TTL не продлевают и копию не обновляют.' },
          { type: 'bullets', items: ['Короткий TTL уменьшает время жизни старой копии, но чаще приводит к чтению базы.', 'Удаление или обновление кэша после записи ускоряет появление свежих данных. Ошибки и гонки при таком обновлении требуют отдельной обработки.', 'Для результата только что выполненной операции можно показать подтверждённый ответ API; для строгой свежести последующего чтения нужен соответствующий путь чтения.', 'Сначала измерьте нагрузку и допустимое устаревание. Если запросы к базе достаточно быстрые, кэш пока может быть не нужен.'] },
          { type: 'callout', text: 'TTL отвечает на вопрос «как долго может жить эта копия», а не «совпадает ли она с базой прямо сейчас». В примере в момент t=21 с у старой записи остаётся 39 секунд срока жизни.' },
        ],
      },
      {
        id: 'design-safe-retry',
        title: '3. Повтор запроса не должен удваивать эффект',
        blocks: [
          { type: 'paragraph', text: 'База успела сохранить выполнение, но ответ потерялся в сети. Клиент видит ошибку и повторяет запрос. По одному сетевому таймауту нельзя понять, было ли действие выполнено. В нашем учебном API клиент сохраняет один ключ идемпотентности для повторов одной команды.' },
          { type: 'code', language: 'text', code: 'Ключ операции: (пользователь, действие, request_key)\n\nBEGIN\n  Сериализовать обработку одинакового ключа\n  Если сохранённая квитанция найдена:\n    Проверить совпадение тела запроса\n    Взять прежний результат без нового начисления\n  Иначе:\n    Применить изменение\n    Сохранить квитанцию с телом и результатом\nCOMMIT\nВернуть результат' },
          { type: 'paragraph', text: 'Квитанция и бизнес-изменение сохраняются атомарно. Ограничение уникальности и сериализация обработки одного ключа защищают от двух одновременных повторов. Если тот же ключ пришёл с другим содержимым, сервер сообщает конфликт. Для новой самостоятельной команды клиент создаёт новый ключ.' },
          { type: 'callout', text: 'Просто отключить кнопку недостаточно: повтор может сделать сеть или другая вкладка. Гарантия относится к спроектированной серверной операции; внешние побочные действия требуют своей защиты. После трёх вопросов будет отмечен демо-модуль, а не освоение полного 16-часового курса.' },
        ],
      },
    ],
    questions: [
      {
        id: 'design-atomic-write',
        prompt: 'В учебном сервисе изменение навыка и запись о выполнении находятся в одной базе. Как сохранить согласованность, если между этими двумя записями возможен сбой?',
        options: [
          { id: 'two-requests', text: 'Разнести записи по двум независимым запросам: сначала навык, позже историю.' },
          { id: 'one-transaction', text: 'Записать оба изменения в одной транзакции и отправить успех после COMMIT.' },
          { id: 'browser-first', text: 'Сначала показать успех в браузере: это гарантирует завершение записи.' },
          { id: 'cache-instead', text: 'Сохранить только значение в кэше с коротким TTL.' },
        ],
        correct_option_id: 'one-transaction',
        explanation: 'Транзакция обеспечивает совместное сохранение двух изменений внутри этой базы либо откат обоих. Ответ после COMMIT подтверждает фиксацию. Независимые записи могут оставить начисленный навык без истории или историю без начисления.',
      },
      {
        id: 'design-cache-age',
        prompt: 'Кэш получил профиль в t=0 с с TTL=60 с. База обновилась в t=20 с; кэш не обновляют, не удаляют и TTL не продлевают. Какое утверждение верно для чтения в t=21 с?',
        options: [
          { id: 'fresh-by-ttl', text: 'Данные обязательно свежие: TTL меньше минуты.' },
          { id: 'expire-at-update', text: 'Запись автоматически истекла в t=20 с из-за изменения базы.' },
          { id: 'stale-possible', text: 'Кэш ещё может вернуть старый профиль; его срок истечёт в t=60 с.' },
          { id: 'new-sixty', text: 'Чтение обязательно перенесёт истечение на t=81 с.' },
        ],
        correct_option_id: 'stale-possible',
        explanation: 'В условиях задачи время истечения задано при заполнении кэша: 0 + 60 секунд. Изменение базы не меняет копию автоматически. В t=21 с она ещё действительна для кэша, хотя уже устарела относительно базы.',
      },
      {
        id: 'design-idempotent-retry',
        prompt: 'Сервер сохранил выполнение и квитанцию, но первый ответ потерялся. Клиент повторил ту же команду с тем же пользователем, ключом и телом. Как должен ответить описанный в модуле API?',
        options: [
          { id: 'repeat-effect', text: 'Начислить эффект ещё раз: каждый HTTP-запрос является новым действием.' },
          { id: 'replay-receipt', text: 'Вернуть сохранённый результат, не применяя эффект повторно.' },
          { id: 'forget-key', text: 'Удалить квитанцию и выполнить команду с новым ключом.' },
          { id: 'trust-button', text: 'Положиться на отключённую кнопку; на сервере ничего проверять не нужно.' },
        ],
        correct_option_id: 'replay-receipt',
        explanation: 'Совпадение ключа и содержания связывает повтор с уже выполненной командой. Сервер возвращает сохранённый результат без второго эффекта. Если содержание под тем же ключом изменилось, это конфликт, а не разрешение выполнить другую команду.',
      },
    ],
    sources: [
      { title: 'PostgreSQL: транзакции и совместное сохранение изменений', url: 'https://www.postgresql.org/docs/current/tutorial-transactions.html' },
      { title: 'Redis: EXPIRE и срок жизни ключа', url: 'https://redis.io/docs/latest/commands/expire/' },
      { title: 'Stripe API: повтор запросов с ключами идемпотентности', url: 'https://docs.stripe.com/api/idempotent_requests' },
    ],
  },
];
