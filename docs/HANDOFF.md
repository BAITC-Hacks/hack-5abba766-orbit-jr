# Передача работы: Career Quest, AI-зона

Обновлено 23 сентября 2026. Работа продолжена в существующем checkout
`D:\Projects\baitc_hackathon\orbit-jr`, ветка `main`, поверх `15c0080`.
Не создавать второй проект или второй package.json в корне.

## Выбранный кейс и источники

Кейс партнёра **Halyk Bank — Career Quest** на HackAlem AI:
профиль сотрудника → 1–3 обоснованных шага развития → выполнение → новые навыки
и траектория → HR-срез. В ТЗ геймификация вторична; основа — качество рекомендаций.

Прочитать:

1. [BACKEND.md](BACKEND.md) — доменные правила, интеграция и ограничения.
2. [contracts/backend.ts](../contracts/backend.ts) — единые типы API и AI.
3. [AI.md](AI.md) — реализованный интерфейс, конфигурация, ошибки и evaluation.
4. [AI_PLAN.md](AI_PLAN.md) — разбор ТЗ, контраргументы и следующие шаги.
5. [ARCHITECTURE.md](ARCHITECTURE.md), [frontend/README.md](../frontend/README.md) — обзор и клиент.

При расхождениях сверять ТЗ, актуальный контракт и фактический код. Старые
счётчики тестов и сообщения коммитов не являются текущим статусом.

## Что уже есть

- Frontend подключён к относительным `/api` по общему контракту; обработаны версии,
  ошибки, loading/fallback, завершение, импорт, HR. Витрина на фикстурах отделена в `/demo`.
- AI-модуль: `recommend(input, { signals, signal, emptyReason })`, адаптер, промпт,
  строгий валидатор, многофакторный baseline, проверка evidence, расчёт истории.
- Бюджет ожидания провайдера до 8 секунд, отключённые retries, отмена, обработка отказа/обрезки.
- Проверки конфликтных случаев и CLI с раздельными offline/live-режимами.
- Общие документы и контракт сохранены; отдельные AI-типы не заведены.

На этой точке ещё отсутствуют серверные HTTP-обработчики, БД/миграции,
полный расчёт навыков/допуска/evidence, авторизация, фактический импорт и Docker-поставка.
Наличие UI для этих действий не означает наличие серверной реализации.
Следующий участник должен перепроверить этот список после fetch: команда работает параллельно.

## Инварианты AI

- Числа, допуск и текст фактов создаёт доменное ядро. Модель выбирает только ID.
- candidate_id отличает конкретную попытку/сессию от event_id; не терять participation_id.
- Минимум три категории доказательств обязательны и для AI, и для fallback.
- Неполные evidence вызывают ошибку построения snapshot, а не красивый ответ без оснований.
- Резервное ранжирование учитывает только оставшийся целевой разрыв; прирост вне цели
  и выше требования не повышает балл. Условная польза prerequisite не становится заработанным навыком.
- Карточки — альтернативные следующие шаги, их эффекты не суммируются.
- Пустой набор требует доменной emptyReason. Не угадывать её внутри AI.
- После await нужна проверка DomainVersion в сервисе, даже при fallback.
- Живой AI, его преимущество и end-to-end скорость пока не подтверждены.

## Что делать дальше

1. Backend строит корректный snapshot и обе карты BaselineSignals; история уже имеет helper,
   полезность открываемых событий рассчитывается по реальному каталогу.
2. Подключить `recommend` с auth, ограничением параллельных запросов и проверкой версии.
3. Добавить ключ в `frontend/.env.local`, выполнить `npm run smoke:ai`, затем live-evaluation.
   Ключ не отправлять в чат, не хранить в Git, не печатать в логах.
4. Проверить полный каталог и отдельный зафиксированный набор профилей;
   измерить релевантность, latency, fallback, а не только валидность JSON.
5. Собрать один настоящий путь до completion/HR/import, затем проверить чистый запуск по README.

## Команды проверки

Из `frontend/`:

```sh
npm test
npm run typecheck
npm run build
npm run eval:ai
```

`npm run smoke:ai` и `npm run eval:ai -- --live` отправляют только авторские синтетические
примеры и требуют ключ. В offline-режиме обращений к провайдеру нет.
Фактически использованное окружение текущей проверки — Windows, Node 24.18.0,
npm 11.16.0. Node 22 и Linux/Docker нужно проверить отдельно перед сдачей.

## Совместная работа и Git

Remote: `https://github.com/BAITC-Hacks/hack-5abba766-orbit-jr.git`.
В этом checkout проверены имя автора `ka1nyy` и GitHub-аккаунт `ka1nyy`.
Локальная Git-почта для следующих коммитов: `252775473+ka1nyy@users.noreply.github.com`.
Ранее настроенная почта не связала коммит `9b577ec` с профилем через GitHub API:
имя автора сохранено, но `author.login` был null. Историю общей main не переписывали.
Для старого коммита привязку можно восстановить подтверждением прежней почты в аккаунте;
эта операция требует доступа владельца к почтовому ящику.
[Правила привязки GitHub](https://docs.github.com/en/account-and-profile/how-tos/email-preferences/setting-your-commit-email-address).
Push использует существующий Windows Credential Manager. Не менять креденшалы,
не добавлять фиктивных соавторов и не публиковать секреты.

Работа идёт в общей `main`. Перед push получить origin и перенести свой коммит поверх
актуального main при необходимости; сохранить чужие изменения и повторить затронутые проверки.
Stage делать по списку собственных файлов. Force-push запрещён.

Исторический PR #1 (`feat/ai-recommendations`) содержит раннюю несовместимую структуру
из корня репозитория и заменён реализацией в main. Его не использовать как источник
актуального кода. Закрытие PR и удаление ветки в рамках этого обновления не выполнялись.

Официальный датасет, локальная БД и `.env*` не добавляются в репозиторий.
Новые тестовые примеры должны оставаться самостоятельно созданными.
